let numeros = [70, 15, 268];
let suma = 0;

for (let i = 0; i < numeros.length; i++) { //forma de recorrer un array
    suma = suma + numeros[i]; //acumula la suma de los elementos
}

console.log("La suma es:", suma); // muestra la suma de todos los elementos